version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    container_name: docker-hello-world
    restart: unless-stopped
    
  # Опционально: можно добавить базу данных или другие сервисы
  # database:
  #   image: postgres:13
  #   environment:
  #     POSTGRES_DB: hello_world
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# Опционально: volumes для базы данных
# volumes:
#   postgres_data: